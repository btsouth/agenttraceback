# Command Code Profile

Command Code is detected through common executable names and application roots.

No stable public semantic database or log format is assumed. The profile reports
semantic import and tailing as unavailable while exposing the generic wrapper
capability. Record Command Code runs with:

```text
agenttraceback run --agent command-code -- command-code
```

Add a semantic parser only when a sanitized, versioned local format can be verified.

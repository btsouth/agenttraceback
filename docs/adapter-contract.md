# Adapter Contract

The maintained contract is
[architecture/adapter-contract.md](architecture/adapter-contract.md). This pointer is
kept because the repository layout reserves `docs/adapter-contract.md` as a stable
documentation path.

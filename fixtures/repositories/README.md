# Repository Fixtures

This directory will hold deterministic Git working-tree fixtures for snapshot,
dirty-file, rename, delete, symlink, ignored-file, and recovery tests. Fixtures must
not depend on the parent repository's Git state.

# Security Fixtures

This directory will hold synthetic secret patterns, sensitive paths, destructive
commands, hostile terminal sequences, malformed parser inputs, path traversal cases,
and symlink-escape cases.

All secrets are intentionally fake and must be safe to commit. Seeded values are used
to prove that default exports, logs, previews, and indexes do not leak them.
